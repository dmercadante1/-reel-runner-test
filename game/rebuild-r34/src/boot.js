'use strict';
(async()=>{const G=GOTHIC,C=GOTHIC_CONFIG;try{if(!window.Phaser)throw Error('Engine unavailable.');for(const a of GOTHIC_ASSETS){const bytes=Uint8Array.from(atob(a.data.split(',')[1]),c=>c.charCodeAt(0));if(bytes.length!==a.bytes)throw Error('Incomplete artwork '+a.key);if(crypto.subtle){const hash=Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256',bytes))).map(x=>x.toString(16).padStart(2,'0')).join('');if(hash!==a.sha256)throw Error('Artwork checksum mismatch '+a.key);}}
 G.game=new Phaser.Game({type:Phaser.AUTO,parent:'game',width:640,height:360,pixelArt:true,antialias:false,roundPixels:true,disableContextMenu:true,physics:{default:'arcade',arcade:{gravity:{y:C.hero.gravity},debug:false}},scale:{mode:Phaser.Scale.FIT,autoCenter:Phaser.Scale.CENTER_BOTH},scene:[GOTHIC_SCENE]});}catch(e){G.fail(e.message);}})();
