# R5 release evidence

This tracked file preserves the evidence directory in a clean checkout. Git does not preserve empty directories.

The browser jobs produce fresh acceptance and playthrough JSON reports and actual screenshots. The release gate checks their success and exact build hashes before copying them here. Only a populated release-gates.json and the separate public-site verification establish release status; this placeholder is not a passing test claim.
