# Scrolling Courtyard — review build 01

User-approved scope: one 10–15-screen Courtyard before expanding other stages; new stage is 7680 logical units / 12 screens, rendered at 1920×1080. Movement 190, stronger jump, holding Film locks facing and reverses run animation when retreating. 21 skeleton/werewolf encounters, active patrols, defensive chip exposure at 20% and short vulnerable openings. New eight-cell-per-enemy atlas, photographic capture echoes, narrow blue exposure beam and perforated film ribbon. Capture 12 to awaken the fountain key near the midpoint; physically collect it, then reach eastern gate. Service passage at x6280 and x3730 connects the later terraces and fountain via Down after quota. Death restarts entire stage and supplies. Every fourth ordinary capture restores one heart. Lower passage and alternate balconies include spare and silver reels.

UI: floating hearts, film strip, textured reserve/pickup reels; no HUD backing panel. Touch D-pad lower left; Film left and Jump right, Super/Reload above. Multi-pointer ownership and D-pad sliding; fullscreen fallback retained. Desktop hides touch controls. Interior-castle window art is outside this Courtyard milestone; no other stages changed.

Verification: 13 focused native checks pass, including input-only traversal from entrance to far end with enemies removed to isolate geometry; arranged enemy states verify quota/key/exit and service passage. Actual in-app Chromium input check passes backward filming, first-enemy capture, and camera scrolling. Landscape touch layout visually reviewed. These are NOT physical-device Safari results. Direct Safari CUA returned 'Computer Use permissions are not granted'; local WebKit process aborted at launch in sandbox. Per user request, no permission interruption and no slow-machine/multi-browser suite. Actual Safari on M3 MacBook Pro and iPhone 16 Pro remains pending. Native engine cannot write its external user log directory; tests write their report inside the project. Exit-only resource cleanup warning does not certify runtime error freedom.

Runtime entry: scenes/courtyard_stage.tscn, scripts/courtyard_stage.gd, isolated Scrolling Courtyard export preset. Shared player and visual additions default off / old speed, preserving historical behavior. Main/source R5 and all earlier web preview files must remain byte-identical. Publish only a new immutable preview directory, exclude local qa.html / QA JavaScript. User artwork is authorized for this separate preview.

Next: user's playfeel/art review of this stage before multiplying layout/animation decisions through other stages. Physical phone thumb comfort, audible sound, difficulty and whole-stage human playthrough remain review items.
